% !Mode:: "TeX:UTF-8"
% 由 tjumain.tex 在导言区（setup/package 之后）载入。
% 这里集中放置从原报告迁移过来的表格与列表版式。
% 注意：caption 宏包的全局 \captionsetup 只能在导言区使用。

%%%%%%%%%% 表格配色（表格沿用原报告的底色） %%%%%%%%%%
\definecolor{WordTableHeader}{HTML}{E5E9EC}
\definecolor{WordTableStripe}{HTML}{F6F7F8}
\definecolor{TableTotal}{HTML}{DDE2E6}
\definecolor{TableText}{HTML}{202428}
\definecolor{WordTableRule}{HTML}{8A939B}

%%%%%%%%%% 表格配色（表格沿用原报告的底色，其余版式改回天大样式） %%%%%%%%%%

%%%%%%%%%% 图表题注（天大样式：五号宋体、居中、编号与题注间空一字） %%%%%%%%%%
\DeclareCaptionFont{tjucaption}{\wuhao\song}
\captionsetup{font=tjucaption,labelsep=quad,justification=centering,singlelinecheck=false,skip=6pt}
\captionsetup[figure]{position=bottom}
\captionsetup[table]{position=top,skip=10pt}

%%%%%%%%%% 表格整体版式 %%%%%%%%%%
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.24}
\setlength{\extrarowheight}{1.3pt}
\setlength{\LTpre}{6pt}
\setlength{\LTpost}{8pt}
\setlength{\LTleft}{0pt plus 1fill}
\setlength{\LTright}{0pt plus 1fill}
\arrayrulecolor{WordTableRule}
\newcommand{\TableHeaderText}[1]{{\color{TableText}\bfseries\fontsize{9.5pt}{12.6pt}\selectfont #1}}
\AtBeginEnvironment{longtable}{\color{TableText}\fontsize{9pt}{12.6pt}\selectfont}

%%%%%%%%%% 原报告中的自定义命令 %%%%%%%%%%
\newcommand{\NumberedPara}[2]{%
  \par\noindent\hangindent=2.45em\hangafter=1%
  \textbf{#1.\ }#2\par
}
\providecommand{\pandocbounded}[1]{\resizebox{\linewidth}{!}{#1}}

%%%%%%%%%% 列表版式 %%%%%%%%%%
\setlist[enumerate,1]{label=\arabic*.,leftmargin=2.35em,labelsep=.55em,itemsep=.25em,topsep=.35em,parsep=0pt}
\setlist[enumerate,2]{label=\alph*.,leftmargin=2.2em,labelsep=.5em,itemsep=.2em,topsep=.25em,parsep=0pt}
\setlist[itemize]{leftmargin=2.2em,labelsep=.55em,itemsep=.2em,topsep=.3em,parsep=0pt}

% 新增证据示意图，与正文表格主色一致。
\definecolor{DiagramMain}{HTML}{123F5A}
\definecolor{DiagramLight}{HTML}{F2F7FA}
\definecolor{DiagramMedium}{HTML}{DCE8EF}
\definecolor{DiagramRule}{HTML}{D6E0E7}

% 跨页表格输出不需要无限收缩胶水；使用仅伸展的填充，
% 避免当前 LaTeX 内核拆分输出盒时触发 Infinite glue shrinkage。
\makeatletter
\AtBeginDocument{%
  \patchcmd{\LT@output}{\vss}{\vfil}{}{\PackageError{report}{Longtable output patch failed}{}}%
  \patchcmd{\LT@output}{\vss}{\vfil}{}{\PackageError{report}{Longtable output patch failed}{}}%
}
\makeatother

% 新增证据示意图，与正文表格主色一致。

% 新增证据示意图，与正文表格主色一致。

% 新增证据示意图，与正文表格主色一致。

% 纵向记录表：记录标题与字段内容一致对齐，记录内部不跨页。
\newcommand{\RecordHeading}[1]{\hspace{5pt}\strut\TableHeaderText{#1}\strut}
% 允许图像就近浮动，避免固定图片造成前一页大面积留白。
\setcounter{topnumber}{2}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{3}
\renewcommand{\topfraction}{.9}
\renewcommand{\bottomfraction}{.85}
\renewcommand{\textfraction}{.1}
\renewcommand{\floatpagefraction}{.75}
\setlength{\floatsep}{12pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{14pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}

\setcounter{LTchunksize}{20} % 保持每组纵向记录内部的分页约束
